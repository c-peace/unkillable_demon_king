# Current State

Last updated: 2026-08-22 (Asia/Seoul)

## Current objective

Harden and evaluate the implemented Lunit L2 multi-turn conversation driver, then prepare a reproducible evaluator-compatible container and final submission branch.

## Current phase

**The three-part semantic-control remediation is implemented but failed its first live activation test.** Synthetic contracts pass, but the live L2 produced zero valid structured plans: all 19 planner attempts in the planning fixed-40 either omitted the terminal tool call or failed schema validation. Consequently the ledger created zero requirements and structured review started zero times. No semantic factor is promoted.

## Workspace status

- Git remote `origin` is connected to `https://github.com/c-peace/unkillable_demon_king.git`.
- The current local branch is `21_45_bestModel` and tracks `origin/21_45_bestModel`.
- The latest fetched submission branch was `origin/lunit/hackathon-submission` at `def09916b1ce14a82bb5f223de59b3aa8583d0ca` before the fail-soft latency changes; a new submission SHA is still required after verification and publish.
- `README.md` is the public architecture, requirements, runbook, and implementation-status document.
- `app/` contains a Python 3.13-compatible, standard-library-only service; root `Dockerfile`, `.dockerignore`, `.env.example`, synthetic tests, smoke test, and Patient Simulator development script now exist.
- The default runtime has no third-party Python dependencies and starts without downloading packages, models, or data.
- `.env` is intentionally tracked for this private submission because the evaluator does not inject credentials. Its value is bundled into the image but is never printed or copied into project documentation, tests, source code, or logs.
- `.env.coeval.local` contains the development judge variable names, is ignored by Git and `.dockerignore`, is not tracked, and was absent from the fresh submission image. Its values were not printed or recorded.
- CoEval is a clean external clone at `/Users/peace/Developer/CoEval`, commit `741263cfafba687f8baeb7422c747ef9557df1c4`, with a 582 MiB Python 3.12 `.venv`; it is not part of the submission build context.
- `evaluation/coeval_lab.py` builds a fresh candidate image, starts it on an isolated host port, runs a fixed one-sample `conquer_val` smoke with bounded retries/concurrency, captures structural logs, audits only aggregate/failure/timing fields, and removes the temporary container. Raw CoEval artifacts remain under Git-ignored `evaluation/runs/`.
- The user has moved `healthBench.pdf` from the repository root into `reference/`; Git currently reports the old path deleted and the `reference/` directory untracked. Preserve this user-owned workspace change until the user chooses to commit it.
- Six PDFs are present under repository-root `reference/`: HealthBench plus the five newly supplied papers. The `.agents/.../references/` directory contains the derived project notes.
- No real API key or patient data is stored in the workspace knowledge files.

## Completed

- Created the repo-scoped `$hackathon-submission` skill.
- Captured the organizer's rules, evaluation process, and submission contract.
- Captured the L2 retrieval/generation architecture and `finalize_retrieval` example.
- Captured Lunit FM, Patient Simulator, and MCP endpoints and authentication conventions without storing credentials.
- Cataloged the 21 supplied MCP tools and their intended data sources.
- Established durable context bootstrap and state-update rules in root `AGENTS.md`.
- Established compaction recovery through `.omx/notepad.md` pointing to this state file.
- Connected the workspace to the GitHub repository. The current working branch remains `init-setting`; the required final submission branch is still `lunit/hackathon-submission`.
- Added `.gitignore` protection for `.omx`, environment secrets, Python caches, and local OS artifacts.
- Reviewed all 39 pages of the public HealthBench paper through text extraction and rendered-page inspection.
- Captured benchmark structure, themes, evaluation axes, interpretation limits, and generalizable L2 harness implications in `references/healthbench-overview.md` without retaining case-specific or grader-exploitation material.
- Reviewed OpenAI's public `simple-evals` HealthBench execution flow at commit `652c89d0ca9df547706735883097e9537d40dc47` and captured interface-safe implications in `references/simple-evals-reference.md` without inspecting benchmark rows.
- Documented the advanced target architecture in `references/advanced-harness-design.md`: progressive execution lanes, request-local clinical state, claim-driven MCP retrieval, evidence registry/provenance verification, and conditional L2 review/revision.
- Reviewed five supplied papers (87 pages total) and captured their generalizable findings, evidence limitations, conflicts, anti-reverse-engineering boundaries, and architecture implications in `references/literature-synthesis.md`.
- Replaced the placeholder root README with a paper-style public design and requirements specification covering required submission/API/L2 contracts, multi-turn context, evidence/RAG behavior, reliability, test/ablation strategy, milestone sequencing, planned source structure, open risks, and final submission gates.
- Implemented `GET /v1/models`, `POST /v1/chat/completions`, OpenAI-shaped errors, stateless full-message handling, optional global request deadlines, privacy-safe traces, and graceful SIGTERM shutdown.
- Implemented the Bearer-authenticated L2 Chat Completions adapter with transient retry, empty-output retry, current and legacy tool-call parsing, and optional native-message or role-labelled case-packet representation.
- Implemented a Streamable HTTP MCP client with initialize/initialized, session handling, JSON or SSE responses, dynamic `tools/list`, `tools/call`, configurable protocol version, and session recovery.
- Implemented the Generation -> `retrieve_relevant_content` -> Retrieval L2/MCP -> `finalize_retrieval` -> Generation path, including source routing, evidence registry, unknown-citation rejection, structural sufficiency validation, and bounded `partial`/`no_evidence` fallback.
- Added a deterministic retrieval-stage transition from `index_get_relevant_nodes` to `index_get_page_content` when the returned node metadata exposes a document id and page range, so L2 no longer has to rediscover the page-read step.
- Implemented feature-flagged high-risk audit and at most one final L2 revision, plus a bounded three-turn Patient Simulator development script.
- Implemented a provenance-validated pre-generation planner with `DIRECT`, `CLARIFY`, `GROUNDED`, and `HIGH_RISK` execution lanes. Full-history deterministic signals can upgrade risk, while malformed plans fall back once to the bounded legacy path with a structural reason.
- Implemented an atomic evidence-requirement ledger. Retrieval calls must name a still-missing requirement, harness-only IDs are stripped before MCP calls, and only known citations with applicability can close a requirement or contribute final evidence.
- Implemented issue-driven structured review. `partial` alone no longer triggers review in the new mode; deterministic contract checks and schema-validated material issues control at most one L2 revision, while malformed review output is recorded as unavailable rather than passed.
- Added cumulative `legacy`, `planning`, `ledger`, and `review` evaluation modes plus non-secret semantic mode fields in manifests and structural logs. All four new runtime flags default to disabled and invalid dependency combinations fail during settings construction.
- Tightened the latency-oriented default runtime after the evaluator failure analysis: the shared request deadline stays disabled by default, retrieval uses compact family routing plus smaller evidence budgets, repeated identical MCP calls are cached within a request, retrieval prompts now expose explicit remaining budgets, and bounded retrieval failure no longer forwards internal error codes into final-generation context.
- Hardened the generation/retrieval boundary against a live L2 edge case: when L2 emits legacy text-encoded tool-call markup instead of standard `message.tool_calls`, the adapter now normalizes it, and the post-retrieval generation prompt explicitly forbids any further tool usage once evidence collection is closed.
- Added intended fail-soft evaluation controls after repeated `coeval_failed` container termination: `.env` specifies one 60-second L2 attempt, no empty-output retry, a 30-second retrieval budget, one generation retrieval, two retrieval-L2 rounds, three MCP calls, and a 4096-token cap. A later configuration audit found that the submitted process does not load these non-secret settings from the bundled file, so they are not submission-image defaults until the configuration path is corrected or the variables are injected into the process environment.
- Verified the development L2 endpoint with a basic request and the standard OpenAI non-streaming tool-call/tool-result continuation contract. The endpoint can return `finish_reason: "stop"` alongside `message.tool_calls`, and the adapter correctly uses the latter as authoritative.
- Discovered all 21 live MCP schemas, negotiated protocol `2025-03-26`, observed successful stateless JSON calls, and validated the structured `index_get_page_content` citation shape.
- Completed a general synthetic guideline request through Generation -> Retrieval L2 -> three MCP calls -> `finalize_retrieval` -> final L2 generation, selecting four evidence items without retaining raw clinical content in project state.
- Ran one input conversation shown publicly in HealthBench paper Figure 1 as a submission-container smoke test. Only the conversation input was used transiently; the published candidate response and case-specific rubric were not used for prompting, scoring, tuning, or repository fixtures.

## Active decisions

- The required deliverable is a containerized OpenAI-compatible backend driver; a UI is not required for evaluation.
- Development time is not constrained to the original 20-hour framing; pursue the advanced design while keeping every layer independently testable, reversible, and subject to runtime ablation.
- Final user-facing content must be generated by Lunit L2.
- Use separate L2 generation and retrieval stages unless evidence supports a better rule-compliant harness.
- Keep the evaluation runtime independent of public internet services and the Patient Simulator.
- Keep conditional review enabled by explicit user decision despite the fixed-40 experiment showing no revisions and worse latency/aggregate score.
- Treat candidate concurrency as an operational parameter to test stepwise (`4 -> 8 -> 12 -> 16 ...`), not as a quality intervention. Increase it only while inference failure rate remains zero and tail latency stays within the evaluator timeout guardrail.
- Do not promote candidate concurrency 20: its fixed-40 trial lost 6/40 requests before they entered the harness. Raising judge concurrency cannot repair candidate-ingress failures; judge tuning remains a separate experiment after candidate inference is stable.
- Treat focused skill references as durable truth and this file as mutable execution state.
- Use HealthBench only as a general response-quality framework; do not reconstruct cases, exact criteria, hidden-set composition, or grader behavior.
- Use a stateless OpenAI-compatible service whose request `messages` array is authoritative; do not add hidden session state without organizer evidence.
- Begin with the organizer-recommended adaptive two-stage L2 design: generation exposes only `retrieve_relevant_content`, retrieval uses bounded MCP access plus `finalize_retrieval`, and the returned user content is generated by L2.
- Treat the current public `simple-evals` implementation as a reference, not proof of the hackathon grader version, optional length adjustment, or complete transport contract.
- For the long-horizon architecture, prefer a deterministic state-machine harness over a free-form autonomous agent loop: conversation-state extraction -> risk/intent routing -> optional adaptive retrieval -> evidence packaging -> L2 answer generation -> compact completeness/safety verification -> final L2 revision when risk warrants it.
- Preserve both the full raw history and a structured clinical working memory with provenance for safety-critical facts; any summarization or compression must never silently alter medications, dosages, allergies, pregnancy, timing, negation, or prior corrections.
- Keep retrieval source selection explicit and authority-ranked by task type, with fallback when router confidence is low, rather than exposing every MCP tool to every question by default.
- Use progressive `DIRECT`, `CLARIFY`, `GROUNDED`, and `HIGH-RISK` lanes so advanced retrieval, verification, and revision run only when their expected benefit justifies their latency and failure surface.
- Keep the global wall-clock request deadline disabled by default. Bound execution with per-call L2/MCP timeouts, retrieval/tool-call budgets, and smaller evidence payloads instead of a hard end-to-end cutoff.
- Treat `cite_uid` provenance and the request-local evidence registry as a core subsystem; never pass arbitrary raw retrieval traces or unknown citation identifiers into the final answer path.
- Use a provenance-linked dual-track response contract: clinical/evidence requirements plus interaction/context requirements.
- Make retrieval gap-aware and sufficiency-driven; continue only for named unsupported claims and bound depth because additional rounds can add noise.
- Never infer answer confidence from retrieval relevance or source tier alone; use answer-level verification and explicit unresolved uncertainty.
- Do not adopt paper-specific specialty branches, exact length targets, hard-coded benchmark anchors, or blanket safety gates.
- Implement SEMA-style interpretation/exploration/adjudication initially inside one retrieval-stage L2 loop backed by a harness-validated Evidence Requirement Ledger; do not multiply L2 agents unless ablation shows a benefit.
- Treat `finalize_retrieval(status="sufficient")` as a validated completion decision over critical evidence requirements, not as a model's unverified impression that search results look adequate.
- Use an approval gate for every optimization loop: evaluate the current harness, report the aggregate result and exactly one proposed change, stop for user approval, then change one factor and rerun the same scope. Do not apply a harness behavior change before approval.
- The north-star objective is aggregate leaderboard/HealthBench Rubric score, not minimizing tool calls or latency in isolation. Inference/scoring failure rate, evaluator timeout compatibility, final-L2 attribution, and P95 latency are guardrails.
- Treat a one-sample smoke only as transport/orchestration verification. Never approve or reject a quality change from that score or trace alone.
- Use the same fixed sample policy and judge for local paired comparisons. Treat local `gpt-4.1` as an optimization proxy rather than proof of the dashboard score because the dashboard dataset, version, and judge are not confirmed identical.
- Promote only a promising fixed-sample candidate to the full published `conquer_val` split, then use the dashboard aggregate result as the evaluator-environment confirmation. Keep one harness factor per approval-gated loop.

## Verification evidence

- Pre-implementation readiness on 2026-08-21: `.env.coeval.local` was non-empty, ignored by Git via `.env.*`, untracked, excluded by `.dockerignore`, and contained non-empty `OPENAI_API_KEY` and `OPENAI_API_BASE` entries without exposing their values.
- Docker Desktop 29.1.5 was running on `aarch64`; an existing `lunit-hackathon-driver-local` container remained untouched on host port 8000.
- `python3 -m unittest discover -s tests -v` passed all 43 tests under local permissions. The sandbox-only socket failures disappeared when the identical suite was rerun with local socket access.
- OpenAI `/v1/models` returned HTTP 200, and a minimal `gpt-4.1` Chat Completions judge request returned HTTP 200 with a valid choices envelope.
- The running harness returned HTTP 200 from `/healthz`, `/v1/models`, and a minimal live L2 chat request with a non-empty OpenAI-shaped response.
- The current MCP client initialized the organizer MCP endpoint and parsed 21 tool schemas.
- `uv` 0.9.17 found a local CPython 3.12.12 runtime. The CoEval repository was not yet cloned, while `git ls-remote https://github.com/lunit-io/CoEval.git HEAD` succeeded at `741263cfafba687f8baeb7422c747ef9557df1c4`.
- A fresh `lunit-harness:readiness` image built from the current checkout and ran on host port 18080 as UID 65534. `/healthz`, `/v1/models`, and live chat returned HTTP 200; `.env.coeval.local` and `/app/evaluation` were absent. The temporary container was then stopped and removed.
- `.env.coeval.local` was tightened from mode `0644` to `0600` while remaining Git-ignored and untracked.
- Homebrew `mise` 2026.8.10 installed successfully. The official `mise run sync` created Python 3.12.14 and the CoEval `.venv` but could not install Linux/NVIDIA-only `nvidia-cutlass-dsl` on macOS ARM.
- The macOS evaluation-only fallback installed CoEval and all non-SGLang runtime/development dependencies in that `.venv`; SGLang is unnecessary because the candidate is the existing OpenAI-compatible container.
- CoEval import and CLI startup succeeded, a one-sample `healthbench_main` candidate configuration composed correctly without contacting models, all 451 CoEval unit tests passed, and `UV_NO_SYNC=1 mise run eval -- --help` confirmed the prepared environment can run without re-triggering the incompatible SGLang sync.
- After adding the development lab and structural logging, `python3 -m unittest discover -s tests -v` passed all 48 tests; targeted ruff checks on every modified/new Python file and `git diff --check` passed.
- The completed `conquer_val` smoke is at `evaluation/runs/20260821T132417Z-smoke`. Candidate and judge concurrency were 1; candidate transport retries were 0; CoEval inference and judge attempts were each 1. CoEval exited 0, produced one unique result, and both inference/scoring failures were zero. Summary and combined-summary counts reconciled.
- The one-sample score was `0.0` and pass rate `0.0`; this smoke is not statistically meaningful and must not be used as a quality baseline. Total CoEval time was 83.47 seconds: candidate generation 65.77 seconds and scoring 17.70 seconds.
- Privacy-safe service events recorded three generation L2 calls, two generation-level retrievals, four retrieval L2 rounds, eight MCP calls, `partial` final retrieval status, two evidence items, 30,640 total tokens, and 64.76 seconds harness latency. No malformed or rejected tool call occurred. Both retrievals repeated the same indexed-node to automatic page-read path twice and each ended `partial`.
- The fixed first-40 `conquer_val` baseline completed at `evaluation/runs/20260821T133652Z-baseline40` with project harness behavior unchanged. The profile fixed candidate concurrency at 4, judge concurrency at 8, candidate transport retries at 0, inference attempts at 1, and judge attempts at 1. CoEval exited 0; all 40 unique samples were evaluated with zero inference and zero scoring failures, and all summary counts reconciled.
- The baseline HealthBench Rubric score was `0.5149594875`, with 25/40 samples at or above the metric threshold and 457 rubric-criterion judge calls. Total wall time was 550.34 seconds. Candidate-service latency was 46.89 seconds mean, 39.24 seconds P50, 102.21 seconds P95, and 137.97 seconds max; the harness used 63 generation L2 calls, 56 retrieval L2 calls, 22 retrievals, 80 MCP calls, and 426,766 total L2 tokens.
- Aggregate axis scores were weakest for context awareness (`0.4471`, 29 applicable samples) and completeness (`0.4472`, 34), followed by instruction following (`0.5789`), accuracy (`0.5997`), and communication quality (`0.7211`). These are local fixed-scope diagnostics, not dashboard score guarantees.
- Retrieval finished `partial` for 20/22 retrieval executions, `no_evidence` once, and `sufficient` once. Five executions hit a retrieval budget, four retrieval tool calls were structurally rejected, and one second generation-level retrieval was rejected after its budget was exhausted. No request failed.
- After adding the baseline profile, the complete 49-test suite passed with local socket permission; targeted ruff checks and `git diff --check` also passed.
- Semantic-control implementation verification on 2026-08-22: the focused 14-test suite and complete 70-test unit/integration suite passed, including localhost HTTP/MCP/server tests. `python3 -m py_compile` on all changed semantic modules and `git diff --check` passed. A fresh Ruff run was unavailable because no `ruff` executable/module is installed in the active Python environment; an earlier targeted Ruff pass preceded the final test addition.
- Live semantic-mode validation on 2026-08-22 ran four one-sample smokes, fresh legacy/planning fixed-40 runs, and ledger/review runs on the identical deterministic first 20 samples. All judge calls completed; fixed-run inference failures were legacy 2/40, planning 3/40, ledger 0/20, and review 1/20, all timeouts.
- On the 17 samples that succeeded in every mode, paired HealthBench Rubric means were legacy `0.4920`, planning `0.5319`, ledger `0.5438`, and review `0.5691`. The deltas versus legacy were `+0.0399`, `+0.0518`, and `+0.0771`, but every bootstrap 95% interval included zero and the semantic modules did not activate successfully, so these differences are not evidence of causal improvement.
- In the planning fixed-40 structural log, 21 requests used the deterministic fast path and all 19 planner calls fell back: 11 missing structured calls, seven value-validation failures, and one type-validation failure. The ledger/review first-20 runs each had nine fast-path requests and the remaining requests fell back to legacy; total validated requirements, structured review starts, and structured revisions were all zero.
- Added `baseline40 --num-samples N` for deterministic bounded-prefix comparisons and `evaluation/analyze_semantic_comparison.py` for privacy-safe paired score/axis/structural aggregation without emitting prompts, answers, or rubric text.
- The approved one-factor experiment changed only the default generation conversation representation from native role messages to the existing explicit role-labelled case packet. A regression test verifies that every turn, role, and correction remains present; the complete suite increased to 50 passing tests and targeted ruff plus `git diff --check` passed.
- The first case-packet fixed-40 run at `evaluation/runs/20260821T135424Z-baseline40` completed all candidate responses but had one judge-side scoring failure, so its 39-sample score was classified as contaminated and was not used as the primary comparison.
- The clean case-packet run at `evaluation/runs/20260821T140252Z-baseline40` evaluated all 40 unique samples with zero inference/scoring failures. Its HealthBench Rubric score was `0.5080185460`, down `0.0069409416` absolute (`-0.6941` percentage points) from the native baseline `0.5149594875`. Thirteen sample scores improved, 22 worsened, and five tied; the paired bootstrap 95% interval for the mean difference was `[-0.1277, 0.1564]`, so this single pair does not establish a statistically reliable global effect.
- The intended axes moved against the hypothesis: context awareness fell from `0.4471` to `0.3638` (`-8.34` points) and completeness from `0.4472` to `0.3896` (`-5.77` points). Accuracy rose `+5.40` points and instruction following `+17.75` points, while communication quality was essentially flat (`-0.38` points).
- Case-packet reduced candidate-service mean latency from 46.89 to 35.12 seconds and P95 from 102.21 to 66.43 seconds, and reduced total L2 tokens from 426,766 to 408,256. It also increased retrievals from 22 to 24, MCP calls from 80 to 84, and retrieval tool rejections from four to seven; no retrieval ended `sufficient` in the clean run versus one under native.
- The next approved one-factor experiment restored native role messages and enabled the existing conditional reviewer for high-risk or `partial`/`no_evidence` responses. The full 50-test suite, targeted ruff, and `git diff --check` passed before evaluation.
- The conditional-review fixed-40 run at `evaluation/runs/20260821T141521Z-baseline40` evaluated all 40 unique samples with zero inference/scoring failures. Its HealthBench Rubric score was `0.4891806738`, down `0.0257788137` absolute (`-2.58` percentage points) from the native baseline. Twelve samples improved, 17 worsened, and 11 tied; the paired bootstrap 95% interval was `[-0.1036, 0.0517]`.
- All 20 eligible responses were reviewed, but none were revised. The reviewer therefore changed no returned answer in this run while adding 796.47 seconds of summed review latency and additional L2 usage. Mean request latency rose from 46.89 to 53.33 seconds, P95 from 102.21 to 132.67 seconds, and total L2 tokens from 426,766 to 452,777.
- Compared with native, conditional review produced mixed axis movement: accuracy `+3.85` points, communication quality `+8.46`, completeness `+1.52`, context awareness `-12.18`, and instruction following `-2.37`. Because no response was revised, these answer-score differences reflect ordinary generation/judge variation rather than a demonstrated review benefit.
- The candidate-only synthetic load test at `evaluation/runs/20260821T143308Z-loadtest` completed 12/12 requests at both tested levels. Concurrency 4 took 211.97 seconds with P95 174.98 seconds; concurrency 8 took 131.54 seconds with P95 131.53 seconds, a 1.611x wall-time speedup in that small synthetic trial.
- The first fixed-100 run at `evaluation/runs/20260821T144043Z-baseline100` used candidate concurrency 8 and judge concurrency 8. It completed 98/100 inference results with two candidate timeouts, zero scoring failures among completed responses, 738.07 seconds wall time, candidate P95 141.33 seconds, P99 220.54 seconds, and maximum 240.15 seconds. Its aggregate score `0.4115531410` is failure-contaminated and not an accepted baseline.
- In that concurrency-8 run, conditional review found 45 eligible responses, reviewed 43 completed responses, and revised three. The harness recorded 195 generation L2 calls, 110 retrieval L2 calls, 47 retrievals, 166 MCP calls, and 1,042,778 total L2 tokens.
- The fixed-100 profile was returned to candidate concurrency 4 and rerun at `evaluation/runs/20260821T145446Z-baseline100`. Only 38/100 inference requests completed; the other 62 all failed with the same external `502 l2_unavailable` response after bounded upstream retries. There were zero judge-side scoring failures, but the run is outage-contaminated and its aggregate score must not be compared with quality baselines.
- CoEval's candidate semaphore and `runner.concurrent_limit=4` were inspected and found to be applied correctly. The 62 failures were not local semaphore bypasses or candidate timeouts; they were uniform upstream L2-unavailable failures after the first 38 completions.
- That concurrency-20 run finished in 162.95 seconds with zero judge failures. Among its 34 completed requests, mean service latency was 53.98 seconds, P50 50.23 seconds, P95 97.37 seconds, and max 157.18 seconds; 17 responses were reviewed and three revised.
- Bottleneck decomposition of the clean reviewer-enabled fixed-40 run found 2,133.01 seconds of summed candidate request time: 1,027.54 seconds generation, 308.96 seconds retrieval, and 796.47 seconds review. Its 123 completed L2 calls consumed 2,095.61 seconds in aggregate (17.04 seconds mean, 53.47 seconds P95), about 98% of summed candidate time; judge scoring averaged 3.89 seconds per evaluated sample. L2 wait time is therefore the dominant latency source, conditional review is the largest optional stage, MCP/driver overhead is secondary, and judge concurrency is not the primary end-to-end bottleneck.
- `baseline40` now accepts a reusable `--candidate-concurrency` override without changing harness or judge behavior. Its seven focused tests passed, targeted ruff from the prepared CoEval environment and `git diff --check` passed, and `python3 -m unittest discover -s tests -v` passed all 55 tests with local socket access. The initial sandbox run's eight socket errors were permission-only and disappeared under the required local permission.
- A privacy-safe structural-log review characterized the current harness independently of score: clean fixed-40 runs sent roughly 43-60% of requests through retrieval; a retrieved request typically used 2.5-2.7 retrieval-L2 rounds and 3.2-3.8 MCP calls, while direct requests used one generation call. Across the clean native fixed-40 run and the structurally informative but failure-contaminated concurrency-8 fixed-100 run, about 83-90% of completed MCP calls were the `index_get_relevant_nodes` plus automatic `index_get_page_content` pair. Explicit `sufficient` completion was rare (1/22 retrievals in the native fixed-40 run and 2/47 retrieval events in the fixed-100 run); most retrievals exited `partial`, often after the model did not call `finalize_retrieval`. Conditional review was selective but low-yield: 20 reviews/0 revisions in the clean fixed-40 run and 43 reviews/3 revisions in the failure-contaminated fixed-100 run. The logs provide strong stage/count/latency observability without raw clinical content, but do not record a structured retrieval rationale, evidence-utilization decision, or reviewer issue category, limiting diagnosis of whether individual tool use was medically necessary.
- Root-cause inspection found that the implemented “lanes” are outcome labels rather than a pre-generation controller: conversation compilation preserves raw turns but creates no clinical/interaction state, high-risk detection regexes only the latest user turn, generation L2 alone decides whether and what to retrieve, the evidence ledger collapses the whole query into one post-hoc requirement, retrieval L2 judges its own sufficiency, and budget fallback forwards the first registered citable items without semantic entailment validation. Review is triggered by the broad `partial`/`no_evidence` state and treats any output not beginning with `REVISE:` as a pass. This explains unstable retrieval entry, predominantly budget-ended `partial` results, and low review yield without attributing run-to-run score variance to those modules.
- Created the planning-only remediation artifact `.omx/plans/harness-semantic-control-remediation.md`. It sequences three independently ablatable changes: provenance-linked clinical/interaction planning, named-gap evidence-requirement retrieval, and structured issue-driven review. Its first phase freezes team-authored synthetic regression contracts; it does not require new organizer information, APIs, MCP tools, dependencies, or benchmark-item inspection.
- An independent critic initially requested four plan clarifications: lane enforcement, valid feature combinations, concrete planner/retrieval interfaces, and semantic trace/manifest fields. After all four were added, the critic returned `APPROVED`; the plan is actionable without major executor guesswork and implementation has not started.

- `python3 /Users/peace/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/hackathon-submission` reported `Skill is valid!` after the continuity setup.
- `python3 -m py_compile app/config.py app/deadline.py app/orchestration/driver.py app/orchestration/retrieval.py app/evidence/routing.py app/prompts.py` completed without syntax errors after the latency-oriented runtime changes.
- `AGENTS.md`, `SKILL.md`, `current-state.md`, and `.omx/notepad.md` were verified to exist and be non-empty.
- `.omx/notepad.md` Priority Context measured 316 characters, below the 500-character limit.
- `git remote -v` confirmed `origin` fetch/push URLs target `c-peace/unkillable_demon_king`.
- `git status --short --branch` confirmed `init-setting...origin/init-setting` tracking during the HealthBench review.
- `git log -1 --oneline` reported `d9ae6f2 init setting : skill 및 논문 학습 (to codex)` during the README specification update.
- `healthBench.pdf` parsed as a 39-page paper; all pages were rendered and visually checked via a contact sheet, with key tables and scoring sections inspected at original resolution.
- `git ls-remote https://github.com/openai/simple-evals.git HEAD` returned `652c89d0ca9df547706735883097e9537d40dc47`; the public `healthbench_eval.py`, runner integration, and chat-completion sampler were inspected without downloading benchmark data.
- All five PDFs under `reference/` were extracted and all 87 pages rendered; complete contact sheets plus original-resolution architecture/result pages were visually checked.
- Root `README.md` contains 838 lines and all expected architecture, requirements, quick-start, implementation-status, and submission sections; `git diff --check` completed without whitespace errors after the implementation update.
- `python3 -m unittest discover -s tests -v` now passes 30 unit/contract/fake integration tests covering multi-turn preservation, direct generation, retrieval/finalizer, citation validation, live page-content normalization, missing-finalizer degradation, retrieval-failure degradation, L2 HTTP retry/tool parsing, optional global-deadline disablement, empty-env fallback, MCP JSON/SSE/session behavior, concurrent stateless API requests, error envelopes, and optional review/revision.
- `python3 -m unittest discover -s tests -v` now passes 35 unit/contract/fake integration tests after adding regression coverage for text-encoded L2 tool-call normalization and the explicit post-retrieval final-generation prompt, covering the same surfaces plus deterministic `index_get_relevant_nodes` -> `index_get_page_content` transition.
- `python3 -m unittest discover -s tests -v` now passes 38 tests after adding fail-soft policy coverage for default budgets, non-retried L2 timeout, bounded retrieval-stage execution, and upstream `max_tokens` propagation.
- The same 38-test suite passed inside the evaluator-aligned `python:3.13-slim` image with the repository mounted read-only.
- The complete 30-test suite passes under the local Python environment; submission-aligned container re-verification is still recommended after any further latency-path changes.
- The latest root Docker no-cache build completed locally in 3.1 seconds with a cached base image; the final image was 43,328,761 bytes, exposed `8000/tcp`, ran as numeric non-root user `65534:65534`, and contained no API key in image configuration/history.
- The built container called a fake host L2 endpoint with Bearer authentication and returned a valid multi-turn OpenAI completion; its privacy-safe trace recorded one L2 call and no raw message content.
- The container started and served `/healthz` successfully with `--network none`, proving startup has no external dependency. SIGTERM shutdown completed with exit code 0.
- A live development basic L2 request returned HTTP `200`, non-empty content, model metadata, and usage. A forced standard function tool call parsed correctly, and a subsequent `role: "tool"` continuation produced final content.
- Live MCP initialization negotiated `2025-03-26`; `tools/list` returned 21 schemas and tested `tools/call` responses contained `content`, `structuredContent`, and `isError` without requiring a session header.
- Fresh verification in the current workspace re-ran the submission-aligned test suite, rebuilt the root Docker image, started the service on `0.0.0.0:8000`, and confirmed `/healthz`, `/v1/models`, a basic L2 completion, a forced non-streaming `tool_calls` response, and a 21-tool MCP discovery plus `cite_uid`-bearing page-content response.
- The live non-streaming tool-call probe returned HTTP `200` with `message.tool_calls`, `finish_reason: "stop"`, and the expected `echo_probe` function call. The live MCP page-content probe returned `cite_uid` and a 1-page `pages` payload for a guideline source.
- The live end-to-end grounded request completed in 90,897 ms with two generation L2 calls, four retrieval L2 calls, three MCP calls, `sufficient` retrieval status, four evidence items, and a non-empty final L2 response. The recorded total usage was 62,626 tokens, making latency and context reduction a priority before enabling broad retrieval by default.
- The rebuilt submission image passed a live development-container check: `/healthz`, a direct L2 answer, 21-tool MCP discovery, a generic MCP call, and a full grounded L2/MCP/L2 request all succeeded with runtime-only credential injection. The first grounded container attempt returned HTTP `502`; a single manual retry succeeded in 54,655 ms with five retrieval L2 calls, five MCP calls, `partial` status, one evidence item, 989 response characters, and 89,084 tokens. This demonstrates the boundary while also exposing upstream variability and excessive retrieval context cost.
- A fresh `lunit-hackathon-driver:healthbench-smoke` no-cache build completed in 1.32 seconds locally. The image ran without manual initialization as `65534:65534`, exposed and bound container port 8000 to host `0.0.0.0:8000`, passed `/healthz`, `/v1/models`, `scripts/smoke_test.sh`, image-history secret scanning, and the complete 24-test Python 3.13 suite.
- The single public-paper smoke conversation returned HTTP `200` in 77.254 seconds with a non-empty L2 assistant response, `finish_reason: stop`, and 2,061 reported tokens. The privacy-safe trace recorded `GROUNDED`, two generation L2 calls, one retrieval request, zero completed retrieval L2/MCP calls, no evidence, and 1,604 response characters. A subsequent isolated MCP discovery from the same container returned 21 tools, so the failed retrieval stage is an observed transient/deadline-path failure rather than proof that container MCP connectivity is absent.
- General HealthBench-axis review, not an official benchmark score: the response used the multi-turn context and asked useful questions about age, feeding, wet nappies, and breathing, but exposed an internal retrieval failure, omitted some high-value red flags such as temperature and color, and made prompt in-person assessment too conditional for a newly less-active or potentially weak infant. Official pediatric guidance treats a floppy, very weak, difficult-to-wake, or non-moving infant as requiring urgent or emergency assessment.
- The exact evaluation failure reported by the user was not reproducible locally from submitted commit `3abc99b08ee4224f0066fbafe02c4e77d1a0d380`: a clean archive build of that SHA started successfully and stayed running on `0.0.0.0:8000` under `docker start --attach`. The evaluator log tail therefore shows a wrapper-level container exit (`exit 1`), but not the underlying container stderr that caused it.
- `docker build -t lunit-hackathon-driver:optimize-check .` completed successfully after the latency-oriented runtime changes.
- `docker run --rm -d -p 18000:8000 --name lunit-optimize-check lunit-hackathon-driver:optimize-check`, `curl http://127.0.0.1:18000/healthz`, `curl http://127.0.0.1:18000/v1/models`, and `docker stop lunit-optimize-check` confirmed that the optimized image still starts automatically and serves the required container boundary.
- A fresh local container run on host port `18004` returned `/healthz` and completed a live grounded request in 46.131 seconds with HTTP `200`, `l2_calls=2`, `retrieval_l2_calls=1`, `mcp_calls=2`, `retrieval_status=partial`, `evidence_count=1`, and `total_tokens=4629`. The final assistant content contained no raw `<tool_call>` markup, confirming that the legacy tool-call normalization plus post-retrieval prompt closed the observed leakage path.
- The submission-equivalent image started successfully under the evaluator's `docker start --attach` pattern. Without a secret, `/v1/models` returned 200 and chat returned a bounded 503 while the process stayed alive; SIGTERM produced exit code 0. A separate 128 MiB container completed 64 concurrent synthetic three-second L2 requests with HTTP 200 and about 28.6 MiB observed memory, weakening startup and ordinary-concurrency OOM hypotheses.
- The fail-soft image rebuilt without cache in 1.58 seconds and started under `docker start --attach`. A live grounded request completed with HTTP 200 in 31.87 seconds: generation 17.36 seconds, retrieval 14.51 seconds, one retrieval L2 call, two MCP calls, one evidence item, and no retries. A separate source-sensitive question that L2 answered without retrieval took 27.90 seconds entirely in one L2 call. These measurements identify L2 latency as the dominant cost and MCP call latency as secondary. SIGTERM again produced exit code 0 with no OOM flag.

## Open questions and blockers

- On macOS ARM, plain `mise run sync` remains incompatible because the upstream default dependency graph includes Linux/NVIDIA SGLang packages. Local evaluation must use the prepared environment with `UV_NO_SYNC=1 mise run eval -- ...` or `.venv/bin/coeval ...`; do not run a normal auto-sync unless upstream adds platform markers.
- The fixed 40-sample baseline is a local paired-comparison reference only. It does not establish the dashboard evaluator's exact dataset/version, judge calibration, or final leaderboard score.
- Judge choice can materially shift absolute HealthBench scores. Local `gpt-4.1` comparisons are useful only when candidate and baseline use identical samples and judge settings; leaderboard impact remains an inference until dashboard confirmation.

- The organizer's expanded advanced tool-calling documentation is still unavailable, although the standard non-streaming request/call/continuation path has been verified directly.
- Complete response, pagination, error, and `cite_uid` coverage across all MCP tools remains unverified; only generic discovery and guideline-source calls were exercised live.
- Evaluation-time secret injection is resolved: none is provided. The image must contain the submission `.env`; evaluator-network L2/MCP connectivity still requires the next dashboard trial.
- Hackathon-specific scoring aggregation, latency limits, and dashboard retry allowance remain unconfirmed; the public paper's HealthBench scoring method is documented separately and must not be assumed to be the organizer's complete scoring contract.
- Live Patient Simulator behavior, streaming, parallel tool calls, context limits, and rate/concurrency limits remain unverified.
- The development L2 endpoint entered a sustained unavailable state during the concurrency-4 fixed-100 retry. Endpoint recovery must be confirmed with a bounded probe before another paid full run; repeated full retries while unavailable would only create contaminated results.
- Concurrency 8 is not currently safe for the 180-second candidate timeout on the real fixed-100 workload because two requests exceeded it. Concurrency 12/16 may be technically configurable, but must not be promoted until concurrency 8 passes a clean representative run or the timeout/latency cause is changed and revalidated.
- Judge concurrency 20 has not been tested because the candidate stage failed first; increasing judge concurrency cannot affect the observed candidate connection failures.
- The tracked `Dockerfile` exposes the team credential in image configuration and Git history. Do not publish or distribute the repository/image as safe until the credential is rotated and the exposure is remediated using an organizer-compatible secret-delivery approach.
- The first full grounded live request took about 91 seconds and 62.6k tokens; retrieval prompt/context compaction and latency budgets require measured optimization.
- The public-paper smoke run completed within the configured deadline but took 77.25 seconds and lost retrieval evidence before final generation. The user-facing response also disclosed the internal retrieval failure and showed possible under-triage; both are release-blocking quality issues for grounded/high-risk paths.
- Earlier CoEval trials used an image without a key, so `/v1/models` succeeded while chat returned immediate `503 service_not_configured`; the wrapper later surfaced only `coeval_failed`. The credential path has now been corrected locally and awaits a new dashboard trial.
- The previous global `REQUEST_TIMEOUT_SEC=90` path is no longer the default bottleneck. Current defaults favor `family` MCP routing, fewer retrieval rounds/tool calls, smaller evidence payloads, and retrieval-step deduplication while keeping per-call upstream timeouts in place.
- The indexed guideline/HIRA retrieval bridge now auto-fetches page content when the relevant node result exposes a document id and page range; the remaining risk is whether live organizer responses use the same metadata keys as the development fake.
- The bundled `submission.env` loader reads only `LUNIT_FM_API_KEY`; it does not load the timeout, retry, retrieval-budget, routing, or review settings present in `.env`. `evaluation/coeval_lab.py` starts the candidate container without `--env-file` or individual candidate environment variables, and the manifests omit effective harness settings. Consequently, current submission-style runs use code defaults (`2` generation retrievals, `4` retrieval rounds, `4` MCP calls, review enabled) rather than the intended fail-soft `.env` profile (`1/2/3`, review disabled), and historical dirty-worktree experiments cannot be reconstructed from their manifests alone.
- `DriverHTTPServer` inherits a listen backlog of five and starts an unbounded daemon thread per request, while the driver has no candidate admission semaphore or upstream circuit breaker. This is consistent with six concurrency-20 connection failures occurring before `request_started`, two concurrency-8 tail timeouts, and the continued attempt storm after the L2 endpoint became unavailable; the logs do not prove the backlog is the sole cause, but they do prove the absence of local load admission and outage suppression.

## Next actions

1. Repair live planner compatibility before any further benchmark run: force or reliably elicit `submit_response_plan`, tighten the nested tool schema to match parser-required fields, and record privacy-safe schema failure categories.
2. Re-run a small team-authored live probe until at least one valid `DIRECT`, `GROUNDED`, and `HIGH_RISK` structured plan is observed; confirm a grounded plan creates named requirements and a high-risk plan reaches structured review.
3. Only after live activation works, repeat the identical deterministic first-20 four-mode comparison and require non-zero structured planner, ledger, and review event counts before interpreting score deltas.
4. Keep the legacy mode as the comparison control; do not promote semantic modes from the current score differences because they came from fast-path or legacy-fallback behavior.
5. After the three factors independently activate and improve the targeted measures, obtain a clean fixed-40 and then one combined fixed-100 run at candidate concurrency 4.

## Update protocol

- Replace the current phase and next actions as work advances; do not append a transcript.
- Record only observed completion and verification evidence.
- Move durable organizer facts into the appropriate focused reference.
- Never record secrets, credentials, or patient data.
