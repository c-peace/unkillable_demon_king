# Current State

Last updated: 2026-08-21 (Asia/Seoul)

## Current objective

Preserve project knowledge across context compaction and new sessions, then begin implementation of the evaluator-compatible Lunit L2 multi-turn conversation driver.

## Current phase

**Repository and pre-implementation setup complete.** Organizer requirements and technical guides are documented; application development has not started.

## Workspace status

- Git remote `origin` is connected to `https://github.com/c-peace/unkillable_demon_king.git`.
- The current local branch is `lunit/hackathon-submission` and tracks `origin/lunit/hackathon-submission`.
- Both remote `main` and `lunit/hackathon-submission` initially point to commit `26775b89263175d3934cc22074e1cf817fad864d`.
- The remote currently contributes only `README.md`; no application source, dependency manifest, tests, or Dockerfile exists yet.
- An existing untracked `healthBench.pdf` was discovered and left untouched pending an explicit decision about whether it belongs in version control.
- No real API key or patient data is stored in the workspace knowledge files.

## Completed

- Created the repo-scoped `$hackathon-submission` skill.
- Captured the organizer's rules, evaluation process, and submission contract.
- Captured the L2 retrieval/generation architecture and `finalize_retrieval` example.
- Captured Lunit FM, Patient Simulator, and MCP endpoints and authentication conventions without storing credentials.
- Cataloged the 21 supplied MCP tools and their intended data sources.
- Established durable context bootstrap and state-update rules in root `AGENTS.md`.
- Established compaction recovery through `.omx/notepad.md` pointing to this state file.
- Connected the workspace to the GitHub repository and checked out the required submission branch with upstream tracking.
- Added `.gitignore` protection for `.omx`, environment secrets, Python caches, and local OS artifacts.

## Active decisions

- The required deliverable is a containerized OpenAI-compatible backend driver; a UI is not required for evaluation.
- Prioritize one reliable vertical slice over optional features within the 20-hour timebox.
- Final user-facing content must be generated by Lunit L2.
- Use separate L2 generation and retrieval stages unless evidence supports a better rule-compliant harness.
- Keep the evaluation runtime independent of public internet services and the Patient Simulator.
- Treat focused skill references as durable truth and this file as mutable execution state.

## Verification evidence

- `python3 /Users/peace/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/hackathon-submission` reported `Skill is valid!` after the continuity setup.
- `AGENTS.md`, `SKILL.md`, `current-state.md`, and `.omx/notepad.md` were verified to exist and be non-empty.
- `.omx/notepad.md` Priority Context measured 316 characters, below the 500-character limit.
- `git remote -v` confirmed `origin` fetch/push URLs target `c-peace/unkillable_demon_king`.
- `git status --short --branch` confirmed `lunit/hackathon-submission...origin/lunit/hackathon-submission` tracking.
- `git log -1 --oneline` reported `26775b8 Initial commit`.

## Open questions and blockers

- Product scenario and target user have not yet been selected.
- Exact advanced L2 tool-calling payload and response examples remain unavailable.
- Live MCP parameter/result schemas and `cite_uid` coverage have not yet been inspected.
- Evaluation-time secret injection and internal L2/MCP connectivity remain unconfirmed.
- Exact scoring rubric, latency limits, and dashboard retry allowance remain unconfirmed.

## Next actions

1. Select the target medical conversation scenario and define a narrow success criterion.
2. Decide whether the existing `healthBench.pdf` is a local-only reference or an intended repository artifact; do not stage it until decided.
3. Scaffold the minimal OpenAI-compatible service with `GET /v1/models` and `POST /v1/chat/completions`.
4. Implement and test the basic L2 generation call before adding retrieval orchestration.
5. Add bounded retrieval/MCP orchestration, multi-turn normalization, citations, Docker packaging, and evaluator-focused tests incrementally.

## Update protocol

- Replace the current phase and next actions as work advances; do not append a transcript.
- Record only observed completion and verification evidence.
- Move durable organizer facts into the appropriate focused reference.
- Never record secrets, credentials, or patient data.
